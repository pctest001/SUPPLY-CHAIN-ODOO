# 供应链管理系统 · 验收与自检清单（面试/验收通用）

> 数据基准：研发任务清单（2026-07-23 末更新，56/56 完成）+ 本清单现场核对（2026-07-24 08:25 起）。
> 结论：**核心 MVP + AI 安全四原则 + 端到端联调全部通过**，作品集可对外展示。
> 配套文档：作品说明.md（叙事）/ 演示走查脚本.md（现场走查）/ README.md（工程）/ 研发任务清单.md（拆项）。

---

## 0. 今日（2026-07-24）现场核对结果

| 项 | 方法 | 结果 |
|----|------|------|
| 环境恢复 | Docker Desktop 启动 → `docker compose up -d` | ✅ odoo + db 容器 Up |
| 登录可达 | `curl http://localhost:8069/web/login` | ✅ HTTP 200（~5s 就绪） |
| 演示数据在库 | psql 直查 `supplychain` | ✅ `RCP/2026/00014` 柠檬味苏打水 330ml 配方；`SACK/2026/00005`（confirmed, 交期 2026-07-30） |
| 回归冒烟 | `docker compose run --rm odoo odoo shell -d supplychain < scripts/h1_e2e.py` | ✅ **H1 联调 35/35 PASS，0 FAIL，0 SKIP**（重启后主链路 + AI 仍全绿） |

---

## 1. 环境与可复现（A 组）

| 验收项 | 标准 | 证据 |
|--------|------|------|
| 一键建库可登录 | `./init.sh` 后 `admin@example.com / admin` 可直接登录 | ✅ post_init_hook 用 Odoo 自身哈希设密码（无二次加密坑） |
| 登录直达应用页 | 非数据库管理器 | ✅ `db-filter=^supplychain$` 锚定单库 |
| 安全默认不削弱 | 登录失败冷却 = Odoo 默认（10 次 / 60 秒） | ✅ 未改 `base.login_cooldown_after` |
| 双模块 + 演示组织 | 华南/华东工厂 + 每公司双仓（HNC2/HNF2/HDR2/HDC2） | ✅ 随模块安装生成 |
| AI 配置 | DeepSeek Key 注入环境变量 + 默认启用配置 | ✅ `SUPPLY_AI_API_KEY` 仅 env、不入库 |

---

## 2. 功能验收（按模块，附自测证据）

| 模块 | 验收标准 | 自测证据 | 状态 |
|------|----------|----------|------|
| B1 物料主数据 | 流程制造标记 auto 批次+效期+可库存 | 见 README #10 | ✅ |
| B2 供应商主数据 | 可建可查，AI 可读 | 原生 res.partner + `query_suppliers` | ✅ |
| B3 配方 BOM | 成品-原料配比+占比、反向展示、约束拦截 | `b3_test.py` **12/12** | ✅ |
| B4 演示数据 | ~22 SKU / 每公司双仓 / 3 临期项 | `b4_verify.py` 实查 4 仓 22 SKU | ✅ |
| C1 PR→PO | 状态机 + 一键转原生 PO | `c1_test.py` **18/18** | ✅ |
| C2 PO 审批流 | 草稿→待审→已批，未批禁止确认/收货 | `c2_test.py` **17/17** | ✅ |
| C3 收货批次效期 | 入向缺批次拒、写 stock.lot 可追溯 | `c3_test.py` **15/15** | ✅ |
| D1 入库/出库 | 按批次量化正确、可追溯 | `d1_test.py` **20/20** | ✅ |
| D2 跨仓调拨 | 事务一致 + 跨仓批次可追溯 | `d2_test.py` **17/17** | ✅ |
| D3 效期拦截 | 过期批次出向被拒（不依赖 is_process_mfg） | `d3_test.py` **12/12** | ✅ |
| D4 负库存/超额收货 | 两类拦截，提示中文 | `d4_test.py` **17/17** | ✅ |
| E1 供应商协同 | 交期确认单 + PO 联动 + 向导 + AI 可查 | `e1_test.py` **15/15** | ✅ |
| F1 库存看板 | 指标卡/各仓/明细/临期/调拨 | `inventory_dashboard.py` 实查 4 仓 32060 件 | ✅ |

> 独立自测合计 **143 项断言全 PASS**（不含 H1 35 项）。

---

## 3. AI 安全四原则验收（G 组差异化点）

| 原则 | 验收点 | 证据 |
|------|--------|------|
| 只读 | 查询走 ORM，继承 `ir.rule` 数据权限 | G4 实测：华东用户 AI 查询 0 条华南泄漏（H1 含该项） |
| 白名单 | 仅 6 个只读工具可调 | 白名单拒绝未知工具=True；工具：`query_stock / query_purchase_orders / query_suppliers / query_expiring_lots / query_low_stock / query_supplier_acks` |
| 密钥不落地 | Key 仅 env，不入库/前端 | `SUPPLY_AI_API_KEY` 在 docker-compose.yml env，无入库 |
| 全链路降级 | LLM 失败主流程不受影响 | 无 Key 调 `ask()` 返回非空降级文案（部署自测） |

| AI 子项 | 状态 | 证据 |
|---------|------|------|
| G1 对话式助手（真实 DeepSeek） | ✅ | `ai_live_test.py` 实调成功；H1 G1 返回非空 |
| G2 预警解读 / G3 补货建议 | ✅ 代码落地 | 规则+LLM 解释已实现，随 G1 链路可用 |
| G4 权限继承 | ✅ 已验证 | #51 + H1 e2e |
| G5 降级 | ✅ 已验证 | 部署自测：无 Key 返回降级文案 |
| G6 注入防护 | ✅ 已验证 | H1 e2e 注入拦截 + 白名单拒绝未知工具 |
| G7 OWL 侧边面板 | ✅ 已落地（2026-07-24） | systray 按钮 + 右侧滑出面板；3 接口经真实 HTTP call_kw 验证；组件完整打包进 web.assets_web.min.js |
| G8 OCR 单据识别 | ⬜ 概念级，二期 | — |

---

## 4. 端到端联调（H1，铁证）

- 命令：`docker compose run --rm odoo odoo shell -d supplychain < scripts/h1_e2e.py`
- 覆盖：B 主数据 → C1 PR→PO → C2 审批 → C3 收货 → D1 入/出库 → D2 调拨 → D3 效期拦截 → D4 负库存/超额 → F1 数据源 → G6 注入拦截 → G1 实时对话 → G4 权限继承
- **结果：35/35 PASS，0 FAIL，0 SKIP**（2026-07-24 重新核对仍全绿；odoo shell 退出即回滚，不污染演示库）

---

## 5. 演示数据就绪（现场可直接点）

| 数据 | 单号/标识 | 位置 |
|------|-----------|------|
| 配方 | `RCP/2026/00014` 柠檬味苏打水 330ml | 供应链管理 / 主数据 / 配方（BOM） |
| 供应商交期确认 | `SACK/2026/00005`（confirmed, 2026-07-30, PO P00024/百世） | 供应链管理 / 供应商协同 / 交期确认 |
| 入库→出库链路 | `WH/IN/00034`(+100) → `WH/OUT/00012`(−30, 批次 BATCH-DEMO-D1) | 库存作业 |
| 跨仓调拨 | `HNCF/00001`（食品级柠檬酸 100, 批次 LOT-HNC2-01） | 库存 / 内部调拨 |
| 过期批次演示 | `LOT-EXPIRED-DEMO`（效期 2025-01-01）+ `HNC2/OUT/00008` | 出库验证即被 D3 拦截 |
| 负库存/超额演示 | `HNC2/OUT/00013`（100>30）/ `D4-PO-OVER-001`（订购5/到货50） | 验证即被 D4 拦截 |
| 临期预警 | 香精香料-A型(35d) / 去离子水(45d) / 消毒液2L(60d) | F1 看板高亮 |

---

## 6. 一键自检（提交/演示前跑）

```bash
cd supply-chain-odoo

# 1) 干净建库可登录（交付评审用）
./init.sh

# 2) 端到端铁证（跑完即回滚，不污染库）
docker compose run --rm odoo odoo shell -d supplychain < scripts/h1_e2e.py
#   预期：H1 联调汇总 PASS=35 FAIL=0 SKIP=0

# 3) 实时看板（另终端）
python3 scripts/inventory_dashboard.py   # 开 http://localhost:5000

# 4) AI 对话返回非空（DeepSeek Key 在 docker-compose.yml 环境变量）
#   顶部/菜单进入 AI 对话 → 问「当前有哪些临期或负库存物料？」
```

- [ ] `./init.sh` 干净建库可登录
- [ ] `scripts/h1_e2e.py` 35/35
- [ ] F1 看板 http://localhost:5000 有数据
- [ ] G1 对话返回非空（DeepSeek Key 在 `docker-compose.yml` 环境变量）

---

## 7. 已知局限与路线图

- G7（OWL 侧边聊天面板）：已于 2026-07-24 落地（顶栏 AI 按钮 + 右侧滑出面板，复用 ask() 与只读/白名单/降级机制）。
- G8（OCR 单据识别）：概念级，二期。
- 看板为独立 Web 服务（直连 psql），下一步可内嵌 Odoo 原生 dashboard 动作。
- AI 仅只读查询，二期可加「规则建议自动生成采购申请」闭环。
- 演示用 DeepSeek Key 已写入本地 `docker-compose.yml`，**勿提交到公开仓库**。
