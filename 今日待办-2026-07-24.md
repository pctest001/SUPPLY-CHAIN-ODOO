# 今日待办 · 2026-07-24（供应链 Odoo MVP 收尾 / 演示录制准备）

## 今日已完成
1. **环境恢复 + 回归打底**：Docker 守护进程拉起 → `docker compose up -d` → `curl /web/login` 200 → 演示数据在库；重跑 `scripts/h1_e2e.py` **35/35 PASS**（重启后主链路 + AI 全绿，odoo shell 退出即回滚不污染库）。
2. **收尾文档**：新建 `验收清单.md`；修正 `作品说明.md` 局限段（B3/E1 已落地，仅 G7/G8 为规划/概念项）；对齐 `供应链管理系统-研发任务清单.md`、`README.md`。
3. **G7 OWL 侧边聊天面板（从零落地）**：`sc_ai` 后端 3 接口（复用 ask） + 前端 OWL 组件（systray 按钮 + Portal 滑出面板） + 资产注册进 `web.assets_backend` 并完整打包进 `web.assets_web.min.js`（攻克 esbuild 不自动内联未列出依赖 JS 的坑）；真实 HTTP 走查通过。`作品说明/任务清单/验收清单/README` 的 G7 行均改 ✅。
4. **演示走查脚本 G7 化**：`演示走查脚本.md` 第 9 步从「向导 UI」改写为「G7 OWL 侧边面板」交互（顶栏气泡按钮 → 滑出面板 → 发问 → AI 回复渲染 → 新对话），并补「录制建议」高光提示；「前置」与「D. 一键自检清单」指向新 PreFlight 脚本。
5. **演示前一键自检脚本 `scripts/demo_preflight.py`**：覆盖 [环境健康]（登录可达 / sc_ai 已装 / G7 接口可用 / G7 组件已打包进后台 bundle / 14 个关键菜单 external id）+ [演示数据齐备]（4 仓 / 配方 RCP / 交期确认 SACK / 过期批次 / LOT-EXPIRED-DEMO / D2 调拨类型）。实测 **15/15 PASS**。演示数据缺失时会打印精确灌数据命令。

## 待办 / 下一步
- **你（本地）**：环境已就绪，按 `演示走查脚本.md` 本地录屏 2–3 分钟即可（A 段铁证 30s + B 段 UI 走查）。录屏前先跑 `demo_preflight.py` 确认 15/15。
- **G8 OCR 单据识别**：概念级，二期，本期不落地。
- 可选：就此交付（环境 + 文档 + 演示脚本均就绪）。

## 备注
- DeepSeek Key 在 `docker-compose.yml` 环境变量 `SUPPLY_AI_API_KEY`（仅环境变量，不入库）。
- 演示登录：`admin@example.com / admin`（PreFlight 每次会把管理员密码置为 admin 并提交）。
- 临时文件 `scripts/_tmp_q2.sql` 为查询草稿，可安全删除。
