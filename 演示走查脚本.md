# 演示走查脚本（照做即可，约 10 分钟）

> 用途：面试 / 验收现场照此点击即可完整演示；也可先跑「自动化铁证」一条命令证明主链路。**B 段为「小白手工测试用例」：纯 UI 点击、逐条带勾选栏，可分发非技术人员照做。**
> 前置：已执行 `./init.sh`（或库已就绪）；**录屏前先跑一键自检**，浏览器开 http://localhost:8069，登录 `admin@example.com / admin`。
>
> ```bash
> cd supply-chain-odoo
> docker compose run --rm odoo odoo shell -d supplychain < scripts/demo_preflight.py
> ```
> 预期：**15/15 PASS**（[环境] 登录可达 / sc_ai 已装 / G7 接口可用 / G7 组件已打包进后台 bundle / 菜单入口齐全；[数据] 4 仓 / 配方 RCP / 交期确认 SACK / 过期批次 / LOT-EXPIRED-DEMO / D2 调拨类型）。
> 任一 FAIL 先修复再录屏——若报「演示数据缺失」，按脚本末尾提示依次运行 `scripts/b4_demo_data.py`、`b3_demo.py`、`d1~d4_demo.py`、`e1_demo.py` 灌入即可。

---

## A. 自动化铁证（建议最先跑，给技术面试官看）

```bash
cd supply-chain-odoo
docker compose run --rm odoo odoo shell -d supplychain < scripts/h1_e2e.py
```

预期：**H1 联调汇总 PASS=35 FAIL=0 SKIP=0**，并打印：
- `D3 拦截提示: ...效期为 2020-01-01，已于今日之前过期，禁止出库`
- `D4 拦截提示: ...现有库存为 50.0，本次作业发出 9999.0 将导致负库存`
- `D4 拦截提示: ...订购量为 10.0 ... 本次到货 20.0，累计将超出订购量`
- `G4 华东用户AI查询未越权看到华南库存(命中华南数=0)`
- `G1 AI 对话返回非空`（DeepSeek 真实回答）

## B. 现场 UI 走查（小白手工测试用例 · 逐条勾选）

> 以下用例**纯 UI 点击**,小白现场照做即可,无需自己敲命令(看板 / AI 等运行环境由部署者提前起好)。
> 每条含「前置 / 步骤 / 预期结果 / 易错提示 / 实测栏」,现场逐条打勾;每条约末「💡 演示话术」保留给讲解者边点边讲。

### 📌 小白必读(通用操作,所有用例通用)
- **地址与登录**:浏览器开 `http://localhost:8069`,账号 `admin` / 密码 `admin`。
- **菜单在哪**:左侧主菜单有两类——
  - 我们定制的业务菜单都在 **「供应链管理」** 下(采购申请、配方、供应商协同、AI 智能等);
  - 库存、采购订单等是 Odoo 自带菜单,在 **「库存」**、**「采购」** 下。
- **建记录**:进到列表页后,点左上角蓝色 **「新建」** 按钮。
- **保存再点按钮**:在表单里填完内容,先点左上 **「保存」**,再去点顶部的彩色动作按钮(蓝色=主要操作)。
- **看状态**:每个表单右上角有一条**彩色状态条**,操作成功会变色(草稿→已确认→完成 等)。
- **搜已有单子**:列表页左上角有搜索框,输入单号(如 `HNC2/OUT/00008`)即可定位。

---

### TC-B01 登录与全中文化
- **前置**:浏览器打开 `http://localhost:8069`,已知账号 `admin / admin`。
- **步骤**:
  1. 打开登录页,看页面文字(应是中文)。
  2. 输入 `admin` / `admin` 登录,看是否直接进到系统主页。
  3. 在顶部搜索框输入「采购」「库存」,看能否搜到中文菜单。
- **预期结果**:
  - 登录页是中文,没有英文单词。
  - 登录后直接进主页(不是让你选数据库的页面)。
  - 顶部能搜到「采购」「库存」等中文菜单。
- **💡 演示话术**:强调界面是中文定制,不是英文原生 ERP。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B02 主数据:流程制造物料
- **前置**:已登录主页。
- **步骤**:
  1. 左侧 **库存 → 产品**,点左上 **新建**。
  2. 在「常规信息」里填 **名称**(如「环氧树脂」)。
  3. 在表单主区域找到 **「流程制造」** 复选框(名称下方附近),勾上它。
  4. 点 **保存**,然后切到 **「库存」页签** 查看。
- **预期结果**:
  - 保存后,「库存」页签里 **「按批次追踪」自动变成「按批次」**,并且 **「有效期」自动勾上**(不用你手动逐项勾)。
- **易错提示**:「流程制造」是我们在主区域加的复选框,不是在「库存」页签里;先勾它,再去「库存」页签看自动变化。
- **💡 演示话术**:勾一个「流程制造」就自动带出批次+效期追溯三件套,体现流程制造领域建模。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B03 采购:采购申请 → 采购订单 → 审批
- **前置**:已登录。供应商、仓库在下拉里直接选一个即可(演示数据里已有,不用新建)。
- **步骤**:
  1. 左侧 **供应链管理 → 采购 → 采购申请**,点 **新建**。
  2. 填表单:**供应商**(下拉选一个)、**仓库**(下拉选一个,如 `HNC2`)。
  3. 切到 **「申请明细」页签**,点明细表下方的 **「添加一行」**:选一个**产品**、填**数量**(如 10),保存。
  4. 点左上 **保存**,再点顶部蓝色 **「提交」** 按钮 → 右上状态条变 **「已确认」**。
  5. **守卫先演示**:此时点顶部 **「确认订单」**(注意:这是采购订单上的按钮,本页没有,需先生成 PO,见下)——
     - 实际上守卫在采购订单上:继续点顶部蓝色 **「生成采购订单」** 生成 PO。
  6. 在打开的**采购订单(PO)表单**上:点顶部蓝色 **「提交审批」** → 审批状态条变 **「待审批」**;再点 **「审批通过」** → 变 **「已通过」**,表单下方「审批信息」出现**审批人 + 时间**。
  7. **守卫演示**:回到这个 PO,在审批状态还是「草稿 / 待审批」时(可点「重置草稿」回到草稿),点顶部 **「确认订单」** → 弹窗报错。
- **预期结果**:
  - PR:点「提交」后状态变「已确认」;点「生成采购订单」后生成 PO,PR 变「完成」。
  - PO:点「提交审批」→「审批通过」后,记录审批人+时间。
  - **未审批时点「确认订单」被拒**,提示「尚未通过审批」。
- **易错提示(重点!)**:**「提交」在采购申请表单,「提交审批 / 审批通过 / 确认订单」在采购订单表单**——是两个不同的页面,别在同一个页面找所有按钮。生成 PO 后,可直接在 PR 表单的「采购订单」页签点 **「查看关联 PO」** 打开 PO,最方便。
- **💡 演示话术**:现场故意在未审批时点一次「确认订单」,展示"不审批不能确认"的护栏。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B04 收货:录批次与效期
- **前置**:TC-B03 里的采购订单已审批通过。
- **步骤**:
  1. 在 PO 表单点顶部 **「确认订单」** → 系统生成一张收货单(入库单)。
  2. 点提示里的收货单进去(或 左侧 **库存 → 作业 → 收货** 找该单)。
  3. 在收货单明细行填 **「批次号」**(如 `BATCH-001`)、**「有效期」**(如 `2027-01-01`),点保存。
  4. 点顶部 **「验证」**(或「标记完成」)→ 收货成功。
  5. 左侧 **库存 → 产品 → 批次/序列号**,搜索 `BATCH-001`,看它是否带着有效期显示。
- **预期结果**:
  - 点「验证」后收货成功;批次/序列号里能搜到 `BATCH-001` 且带有效期(后续出库可追溯)。
- **易错提示**:流程制造物料**必须填批次号+有效期**,漏填点「验证」会被拦;这是系统的防呆,不是你操作错。
- **💡 演示话术**:批次+效期进了系统,以后任何出库都能溯源到这一批。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B05 出库效期拦截(重点演示)
- **前置**:演示数据已灌入,库里有一个**已过期批次** `LOT-EXPIRED-DEMO`(效期 2025-01-01),且已绑定出库单 `HNC2/OUT/00008`。
- **步骤**:
  1. 左侧 **库存 → 作业 → 交货**,在搜索框输入 `HNC2/OUT/00008` 打开该单。
  2. 点顶部 **「验证」**(或「标记完成」)。
  3. 观察是否弹窗报错。
  4. (可选对照)再打开一个用**有效批次**的演示交货单 `WH/OUT/00012`,点「验证」→ 成功。
- **预期结果**:
  - `HNC2/OUT/00008` 点「验证」被拒,提示类似「效期已过,禁止出库」。
  - `WH/OUT/00012`(有效批次)验证成功,库存按批次扣减。
- **易错提示**:直接搜演示单号打开即可,不用自己新建出库单、不用自己绑批次——那是给开发者的步骤。
- **💡 演示话术**:这是核心防呆点——过期原料自动被拦,不让出库。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B06 跨仓调拨
- **前置**:演示数据已有内部调拨单 `HNCF/00001`(食品级柠檬酸,批次 `LOT-HNC2-01`)。
- **步骤**:
  1. 左侧 **库存 → 作业 → 内部调拨**,搜索 `HNCF/00001` 打开。
  2. 点顶部 **「验证」** → 成功。
  3. 左侧 **库存 → 产品 → 批次/序列号**,搜 `LOT-HNC2-01`,看它在**目的仓**也出现了记录。
- **预期结果**:
  - 调拨验证成功;批次 `LOT-HNC2-01` 在目的仓也能查到(跨仓批次跟随)。
- **💡 演示话术**:调拨不是简单加减,批次级追溯跨仓也保持。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B07 负库存 / 超额收货拦截
- **前置**:演示数据已有两张单——出库单 `HNC2/OUT/00013`(发出 100 > 库存 30)、采购订单 `D4-PO-OVER-001`(订购 5 / 演示到货 50)。
- **步骤**:
  1. **负库存**:左侧 **库存 → 作业 → 交货**,搜 `HNC2/OUT/00013` 打开,点 **「验证」** → 应被拒。
  2. **超额收货**:左侧 **采购 → 订单 → 采购订单**,搜 `D4-PO-OVER-001` 打开,点 **「确认订单」** 生成收货单;进收货单(明细到货量已是 50),点 **「验证」** → 应被拒。
- **预期结果**:
  - `HNC2/OUT/00013` 验证被拒(发出量超过现有库存,负库存护栏)。
  - `D4-PO-OVER-001` 收货验证被拒(到货累计超过订购量,超额收货护栏)。
- **易错提示**:两张单都是预置演示单,直接搜单号打开点「验证」即可,不用自己改数量。
- **💡 演示话术**:两类异常都被拦,提示是中文,清楚说明原因。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B08 实时库存看板
- **前置**:部署者已启动看板服务,浏览器可直接访问 `http://localhost:5000`。
- **步骤**:
  1. 打开 `http://localhost:5000`,查看页面上的 4 个仓库指标卡、各仓库存汇总、实时明细、临期预警、跨仓调拨。
- **预期结果**:
  - 页面有数据,4 仓指标卡与临期预警正常展示(临期项会高亮)。
- **💡 演示话术**:这是给管理者的实时视图,临期物料一眼可见。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B09 嵌入式 AI 侧边面板
- **前置**:DeepSeek Key 已在 `docker-compose.yml` 环境变量;已登录。
- **步骤**:
  1. 点页面**顶栏最右侧**的 **AI 助手按钮(气泡图标 `💬`)** → 右侧滑出聊天面板。
  2. 在输入框问「当前有哪些临期或负库存的物料?给建议」→ 按 **回车** 发送(Shift+Enter 是换行)。
  3. 点面板右上 **「新对话」** 可清空并开启新一轮。
- **预期结果**:
  - 面板从右侧滑出,并自动载入你最近的会话(不用离开当前页面)。
  - AI 基于真实库存数据给出回答,消息以气泡形式显示。
- **易错提示**:按钮在**顶栏最右边**,不是左侧菜单;发消息用回车,不是点按钮(没有发送按钮)。
- **💡 演示话术**:强调这是**嵌入 Odoo 原生界面的组件**,不是外挂网页;它只做只读查询、走白名单、继承你的数据权限、密钥不落地、出问题时自动降级。也可从 左侧 **AI 智能 → 对话助手(提问)** 走原生向导,共用同一套后台。
- ☐ 实测通过　☐ 实测失败(备注:______)

## C. 录制建议

- 先录 A 段（命令行铁证，30 秒），再录 B 段 UI 走查（8–10 分钟）。
- 叙事主线：「通用 ERP 不防呆 → 我加了批次效期守卫 → 再让管理者用自然语言查异常，且 AI 绝不越权」。
- **AI 部分（第 9 步）是体验高光**：点顶栏气泡按钮、面板滑出、实时给出基于真实库存的建议——强调这是 Odoo 原生 OWL 组件而非外挂页，且复用同一份只读安全策略。
- 收尾回 `作品说明.md` 的 STAR 速记，强调 35/35 联调与 0 越权结果。

## D. 一键自检清单（录屏前必跑）

```bash
cd supply-chain-odoo
docker compose run --rm odoo odoo shell -d supplychain < scripts/demo_preflight.py
```

预期：全部 **PASS**，打印 `PASS=N  FAIL=0  TOTAL=N`。覆盖的检查项：
- 系统管理员存在、演示密码已置为 `admin`（录屏登录用）。
- 演示数据在库（`RCP/2026/00014` 配方、`SACK/2026/00005` 采购单、批次与库存作业计数 > 0）。
- `sc_ai` 模块已安装。
- **G7 后端接口可用**：`get_or_create_session` / `chat`(返回非空) / `new_session` 直接调用无异常。
- **G7 前端组件已完整打包进后台 bundle**（`web.assets_web.min.js` 含 `_loadSession` / `get_or_create_session` / `new_session` / `sc_ai_chat` 方法体）——这一项是「侧边面板能否渲染」的真正保证，漏打包会静默不渲染。
- 关键菜单 external id 全部存在（顶栏/库存/采购/AI 智能等走查要点的入口）。

> 任一 FAIL 先修复再录屏，避免现场卡壳。另：`scripts/h1_e2e.py`（35/35）与 F1 看板（http://localhost:5000 有数据）维持原有要求。
