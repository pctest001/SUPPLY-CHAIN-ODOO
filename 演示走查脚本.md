# 演示走查脚本（照做即可，约 10 分钟）

> 用途：面试 / 验收现场照此点击即可完整演示；也可先跑「自动化铁证」一条命令证明主链路。**B 段为「小白手工测试用例」：纯 UI 点击、逐条带勾选栏，可分发非技术人员照做。**
> 前置：已执行 `./init.sh`（或库已就绪）；**录屏前先跑一键自检**，浏览器开 http://localhost:8069，登录 `admin@example.com / admin`。
>
> ```bash
> cd supply-chain-odoo
> docker compose run --rm odoo odoo shell -d supplychain < scripts/demo_preflight.py
> ```
> 预期：**15/15 PASS**（[环境] 登录可达 / sc_ai 已装 / G7 接口可用 / G7 组件已打包进后台 bundle / 菜单入口齐全；[数据] 4 仓 / 配方 RCP / 交期确认 SACK / 过期批次 / LOT-EXPIRED-DEMO / D2 调拨类型）。
> 任一 FAIL 先修复再录屏——若报「演示数据缺失」，按脚本末尾提示依次运行 `scripts/b4_demo_data.py`、`b3_demo.py`、`d1~d4_demo.py`、`e1_demo.py` 灌入即可。

---

## A. 自动化铁证（建议最先跑，给技术面试官看）

```bash
cd supply-chain-odoo
docker compose run --rm odoo odoo shell -d supplychain < scripts/h1_e2e.py
```

预期：**H1 联调汇总 PASS=35 FAIL=0 SKIP=0**，并打印：
- `D3 拦截提示: ...效期为 2020-01-01，已于今日之前过期，禁止出库`
- `D4 拦截提示: ...现有库存为 50.0，本次作业发出 9999.0 将导致负库存`
- `D4 拦截提示: ...订购量为 10.0 ... 本次到货 20.0，累计将超出订购量`
- `G4 华东用户AI查询未越权看到华南库存(命中华南数=0)`
- `G1 AI 对话返回非空`（DeepSeek 真实回答）

## B. 现场 UI 走查（小白手工测试用例 · 逐条勾选）

> 以下用例**纯 UI 点击**,小白现场照做即可,无需自己敲命令(看板 / AI 等运行环境由部署者提前起好)。
> 每条含「前置 / 步骤 / 预期结果 / 实测栏」,现场逐条打勾;每条约末「💡 演示话术」保留给讲解者边点边讲。

### TC-B01 登录与全中文化
- **前置**:浏览器打开 `http://localhost:8069`,已知账号 `admin / admin`。
- **步骤**:
  1. 打开登录页,观察页面文案(应无英文残留)。
  2. 输入 `admin / admin` 登录,观察是否直达应用页。
  3. 顶部搜索框输入「采购」「库存」,确认中文菜单可见。
- **预期结果**:
  - 登录页全中文,无英文残留。
  - 登录后直达应用主页(非数据库管理器)。
  - 顶部可搜到「采购」「库存」等中文菜单。
- **💡 演示话术**:强调这是领域化定制,菜单全中文化。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B02 主数据:流程制造物料
- **前置**:已登录应用主页。
- **步骤**:
  1. 进入 库存 → 产品 → 新建。
  2. 名称填「环氧树脂」,勾选「流程制造」。
  3. 保存,再打开该物料查看追踪设置。
- **预期结果**:
  - 保存后自动开启「按批次追踪」+「效期管理」+「可库存」(领域驱动,无需手动逐项勾)。
- **💡 演示话术**:勾一个「流程制造」就自动带出三件套,体现流程制造领域建模。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B03 采购:PR → PO → 审批
- **前置**:已登录,已知某供应商与仓库。
- **步骤**:
  1. 采购 → 采购申请 → 新建,选供应商、仓库,加一行物料 → 保存 → 提交。
  2. 点「生成采购订单」,打开生成的 PO。
  3. PO 上点「提交审批」→「审批通过」,观察是否记录审批人/时间。
  4. 回到 PO 未审批状态,尝试点「确认订单」。
- **预期结果**:
  - 提交后状态变待审批;审批通过后记录审批人/时间。
  - **未审批时点「确认订单」被拒**,提示需先审批(C2 守卫)。
- **💡 演示话术**:现场故意在未审批时点一次「确认订单」,展示守卫拦截。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B04 收货:批次 / 效期录入(C3)
- **前置**:TC-B03 的 PO 已审批。
- **步骤**:
  1. PO 点「确认订单」→ 生成收货作业 → 入库(收料)。
  2. 明细行录入「批次号」(如 BATCH-001)+「效期」→ 验证。
  3. 进入 库存 → 批次/序列号,查找该批次。
- **预期结果**:
  - 验证成功;批次/序列号中可查到该 lot 且带效期(可追溯)。
- **💡 演示话术**:批次+效期入系统,后续任何出库都能溯源。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B05 出库效期拦截(D3,重点)
- **前置**:库内有一个**已过期**批次(演示数据 `LOT-EXPIRED-DEMO`,效期 2025-01-01)。
- **步骤**:
  1. 新建出库单,绑定该过期批次 → 验证。
  2. 换成**有效**批次 → 验证。
- **预期结果**:
  - 过期批次验证被拒,提示「效期已过,禁止出库」。
  - 有效批次验证成功,库存按批次精确扣减。
- **💡 演示话术**:这是核心防呆点——过期原料自动被拦,不让出库。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B06 跨仓调拨(D2)
- **前置**:存在两仓库存(演示数据 `HNCF/00001` 食品级柠檬酸)。
- **步骤**:
  1. 库存 → 作业类型用「内部调拨(HNCF)」,原料仓→成品仓,带批次 → 验证。
- **预期结果**:
  - 事务一致,批次自动跟随到目的仓(跨仓可追溯)。
- **💡 演示话术**:调拨不是简单加减,批次级追溯跨仓保持。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B07 负库存 / 超额收货拦截(D4)
- **前置**:已知某物料现有库存;新建一个 PO。
- **步骤**:
  1. 出库单发出量 > 现有库存 → 验证。
  2. 新建 PO 订购 10,收货到货 20 → 验证。
- **预期结果**:
  - 出库超出库存被拒(负库存守卫)。
  - 收货超出订购量被拒(超额收货守卫)。
- **💡 演示话术**:两类异常都被拦,提示中文清晰。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B08 实时库存看板(F1)
- **前置**:部署者已启动看板服务,浏览器可直接访问 `http://localhost:5000`。
- **步骤**:
  1. 打开 `http://localhost:5000`,查看 4 仓指标卡、按仓汇总、实时明细、临期预警、跨仓调拨。
- **预期结果**:
  - 页面有数据,4 仓指标卡与临期预警正常展示。
- **💡 演示话术**:这是给管理者的实时视图,临期项高亮。
- ☐ 实测通过　☐ 实测失败(备注:______)

### TC-B09 嵌入式 AI 侧边面板(G7)
- **前置**:DeepSeek Key 已在 `docker-compose.yml` 环境变量;已登录。
- **步骤**:
  1. 顶栏**右侧**点击 **AI 助手按钮(气泡图标 `💬`)** → 右侧滑出聊天面板。
  2. 输入框问「当前有哪些临期或负库存的物料?给建议」→ 回车发送(Shift+Enter 换行)。
  3. 点面板右上「新对话」清空并开启新会话。
- **预期结果**:
  - 面板滑出并自动载入最近会话(无需离开当前页)。
  - AI 基于真实数据返回非空回答,消息以气泡渲染。
- **💡 演示话术**:强调这是**嵌入 Odoo 原生界面的 OWL 组件**(systray + Portal 滑出),不是外挂网页;底层复用 G1 的 `ai.chat.session.ask()`,继承全部安全设计:AI 仅只读、走白名单工具、继承用户权限、Key 不落地 env、失败降级。也可从 菜单 → AI 智能 → 对话助手 走原生向导,共用同一后端同一份安全策略。
- ☐ 实测通过　☐ 实测失败(备注:______)

## C. 录制建议

- 先录 A 段（命令行铁证，30 秒），再录 B 段 UI 走查（8–10 分钟）。
- 叙事主线：「通用 ERP 不防呆 → 我加了批次效期守卫 → 再让管理者用自然语言查异常，且 AI 绝不越权」。
- **AI 部分（第 9 步）是体验高光**：点顶栏气泡按钮、面板滑出、实时给出基于真实库存的建议——强调这是 Odoo 原生 OWL 组件而非外挂页，且复用同一份只读安全策略。
- 收尾回 `作品说明.md` 的 STAR 速记，强调 35/35 联调与 0 越权结果。

## D. 一键自检清单（录屏前必跑）

```bash
cd supply-chain-odoo
docker compose run --rm odoo odoo shell -d supplychain < scripts/demo_preflight.py
```

预期：全部 **PASS**，打印 `PASS=N  FAIL=0  TOTAL=N`。覆盖的检查项：
- 系统管理员存在、演示密码已置为 `admin`（录屏登录用）。
- 演示数据在库（`RCP/2026/00014` 配方、`SACK/2026/00005` 采购单、批次与库存作业计数 > 0）。
- `sc_ai` 模块已安装。
- **G7 后端接口可用**：`get_or_create_session` / `chat`(返回非空) / `new_session` 直接调用无异常。
- **G7 前端组件已完整打包进后台 bundle**（`web.assets_web.min.js` 含 `_loadSession` / `get_or_create_session` / `new_session` / `sc_ai_chat` 方法体）——这一项是「侧边面板能否渲染」的真正保证，漏打包会静默不渲染。
- 关键菜单 external id 全部存在（顶栏/库存/采购/AI 智能等走查要点的入口）。

> 任一 FAIL 先修复再录屏，避免现场卡壳。另：`scripts/h1_e2e.py`（35/35）与 F1 看板（http://localhost:5000 有数据）维持原有要求。
