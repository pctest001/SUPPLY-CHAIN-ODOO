[project]
name = "supply-chain-odoo-tests"
version = "0.1.0"
description = "自动化测试工程：以 RPC 黑盒驱动 supply-chain-odoo"
requires-python = ">=3.10"
dependencies = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
