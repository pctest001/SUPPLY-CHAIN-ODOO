# 供应链管理系统（Odoo 二次开发）· 作品说明

> 面试作品集 / 全栈 + AI 叙事。定位：1 人 / 1 周，基于 Odoo 18 二次开发，叠加对话式 AI 助手。
> 代码基准：2026-07-23。验证结论：**H1 端到端联调 35/35 通过，主链路 + AI 一次跑通。**

---

## 1. 一句话定位

为「流程制造」（化工 / 食品等需批次 + 效期追溯）场景，在 Odoo 18 上落地一套轻量供应链系统：
**主数据 → 采购 → 多仓库存 → 批次效期守卫 → 实时看板 → 对话式 AI 助手**，
并以「安全四原则」约束 AI 能力——证明我能把通用 ERP 平台改造成带行业约束、可审计、可对话的产品。

## 2. 为什么值得做（痛点 / 场景）

- 流程制造的核心诉求是**批次可追溯 + 效期防呆**：过期原料绝不允许发往产线。
- 通用进销存（含 Odoo 原生）默认只做「数量」，不做「批次效期业务约束」。
- 管理者要的往往是「用自然语言问库存 / 异常」，而不是翻 10 张表。
- 作品集要展示的不是「又搭了个 CRUD」，��是：**领域约束建模 + 守卫式拦截 + AI 安全集成**。

## 3. 技术栈与架构

| 层 | 选型 | 说明 |
|----|------|------|
| 平台 | Odoo 18.0 Community | 复用采购/库存/产品原生模型，避免重复造轮子 |
| 数据库 | PostgreSQL 15 | 标准库，Odoo 原生 |
| 编排 | Docker Compose | 一键起环境（`db` + `odoo` 两容器） |
| 核心模块 | `supply_chain_demo` | 主数据/采购/多仓/批次效期守卫 |
| AI 模块 | `sc_ai` | 对话式助手 / 预警解读 / 补货建议（DeepSeek + Function Calling） |
| 看板 | Python 标准库 `http.server` + 直连 `psql` | 零三方依赖，实时指标卡 |

**架构要点**：所有业务约束都收敛到 `stock.picking.button_validate` 这一个守卫点
（C3 收货批次效期 / D1 出库批次 / D2 调拨 / D3 效期 / D4 负库存与超额收货），
一处拦截、全链路生效，避免规则散落。

## 4. 功能模块地图

| 分组 | 内容 | 状态 |
|------|------|------|
| A 基础设施 | docker-compose / 双模块安装 / 2 公司 4 仓演示组织 / 可复现初始化 / AI Key | ✅ |
| B 主数据 | 物料 + 流程制造标记 / 演示数据(~22 SKU) / 配方 BOM(成品-原料配比+占比) / 供应商主数据 | ✅ |
| C 采购 | PR→PO / PO 审批流(草稿→待审→已批) / 收货批次效期录入 | ✅ |
| D 库存(多仓) | 入库出库 / 跨仓调拨 / 效期拦截 / 负库存+超额收货拦截 | ✅ |
| E 供应商协同 | 供应商交期确认单(待确认/已确认/已驳回) / PO 协同状态联动 / 登记向导 / 协同工作台 / AI 可查 | ✅ |
| F 看板 | 实时库存看板(按仓/批次/临期) | ✅ |
| G AI 智能 | 对话助手 / 预警解读 / 补货建议 / 注入防护 / 降级 / 权限继承 / OWL 侧边面板 | ✅（G7 OWL 侧边聊天面板已落地；G8 OCR 概念级，二期） |
| H 交付 | 端到端联调 + 作品说明 + 演示走查 | ✅ 本文件 |

## 5. 工程亮点（按面试官关注点组织）

### 5.1 批次 / 效期全链路可追溯
- 标记「流程制造」即自动开启 `tracking=lot` + `use_expiration_date` + `is_storable`（领域驱动）。
- 收货时录入批次号 + 效期 → 写入 `stock.lot`，后续出库按 `lot_id` 绑定到具体收货批次，
  库存量化(write-off)按批次精确扣减，实现「哪批进、哪批出」全程可追。

### 5.2 多仓库存 + 守卫式拦截（D1–D4）
- **D1** 入库/出库：按批次量化正确变动。
- **D2** 跨仓调拨：内部调拨原生携带批次，事务一致 + 跨仓批次自动可追溯。
- **D3** 效期拦截：出向作业绑定「过期批次(lot)」时拒绝验证——**不依赖 `is_process_mfg`，凡过期一律拦**。
- **D4** 负库存 / 超额收货拦截：发出量 > 来源库位现有库存即拒；累计收货 > 订购量即拒。
- 拦截统一以 `UserError` 返回中文提示，UI 与 API 同源。

### 5.3 AI 智能层的安全四原则（作品集差异化点）
1. **只读**：所有查询走 Odoo ORM，自动继承当前用户 `ir.rule` 数据权限（不越权）。
2. **白名单**：LLM 仅可调用 6 个只读工具（`query_stock / query_purchase_orders / query_suppliers / query_expiring_lots / query_low_stock / query_supplier_acks`），防提示词注入与越权写。
3. **密钥不落地**：API Key 仅从环境变量 `SUPPLY_AI_API_KEY` 读取，绝不入库 / 硬编码 / 进前端。
4. **全链路降级**：LLM 调用失败 / 超时，主流程不受影响，提示「AI 暂不可用」+ 规则引擎兜底。

### 5.4 可复现的工程化
- `./init.sh` 一条命令建库 + 装模块 + 设置可直接登录的 `admin`（由模块 `post_init_hook`
  用 Odoo 自身哈希设置，不削弱登录冷却等安全默认）。
- 自测脚本统一在 `odoo shell` 事务内运行、退出即 `rollback`，**不污染演示库**，可重复回归。

## 6. 端到端业务流（主链路）

```
B 物料(流程制造) → C1 采购申请PR→PO → C2 PO审批(待审→已批)
   → C3 收货(录批次+效期) → D1 入库(+100)
   → D2 跨仓调拨(原料仓→成品仓, 事务一致)
   → D1 出库(绑定有效批次, -30)
   → D3 过期批次出库被拦截 ❌
   → D4 发出>库存 被拦截 ❌ / 累计收货>订购 被拦截 ❌
   → F1 看板直读 stock.quant 展示跨仓分布
   → G1 AI 自然语言问答(真实 DeepSeek 调用)
```

## 7. 验证结果

- **H1 端到端联调**：`scripts/h1_e2e.py`，`odoo shell` 内一条龙跑通主链路 + AI，
  **35/35 PASS**，覆盖 B/C1/C2/C3/D1/D2/D3/D4/F1 数据源/G1 实时对话/G6 注入拦截/G4 权限继承。
- 各模块独立自测：C1/C2/C3/D1/D2/D3/D4 均 12–20 项通过；AI 工具层 / 降级 / 临期健壮性已验证。
- **G4 权限继承实测**：以华东工厂用户身份调用 AI 工具，仅看到华东库存，0 条华南数据泄漏。

## 8. 如何运行

```bash
cd supply-chain-odoo
./init.sh                       # 建库+装模块+设 admin
# 浏览器访问 http://localhost:8069  → admin@example.com / admin

# 端到端联调（自动化铁证，跑完即回滚，不污染库）
docker compose run --rm odoo odoo shell -d supplychain < scripts/h1_e2e.py

# 实时库存看板
python3 scripts/inventory_dashboard.py   # 开 http://localhost:5000
```

## 9. 面试叙事（STAR 速记）

- **S**：流程制造企业需要批次效期防呆 + 管理者自然语言查异常。
- **T**：1 周内在 Odoo 上交付一套带行业约束、可审计、可对话的轻量供应链。
- **A**：① 领域建模（流程制造自动开启批次效期）；② 守卫点收敛到 `button_validate` 统一拦截；
  ③ AI 用 Function Calling 白名单 + 继承 `ir.rule` + Key 不落地 + 降级，保证「能对话但不越权」；
  ④ 工程化：一键初始化 + 事务内自测回滚。
- **R**：主链路 + AI 一次跑通（35/35），过期料零流出、超额零收货、AI 零越权。

## 10. 局限与下一步

- G8（OCR 单据识别）为概念项，本期未落地；G7 OWL 侧边聊天面板、B3（配方 BOM）、E1（供应商协同交期确认）均已落地并自测通过。
- 看板为独立 Web 服务，下一步可内嵌 Odoo 原生 dashboard 动作。
- AI 仅只读查询，二期可加「基于规则的建议自动生成采购申请」闭环。
